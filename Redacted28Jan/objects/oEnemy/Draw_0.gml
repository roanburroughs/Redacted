// Inherit the parent event
event_inherited();

if (hsp<0)
	{
	    //image_xscale = abs(image_xscale)
	}
	else
	{
	    //image_xscale =-abs(image_xscale)
	}

image_blend = c_white;
if (vulnerable == true)
{
	image_blend = paintedcolour;
}
